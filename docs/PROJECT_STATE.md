# 작업 기록 — 2026-08-08

> 근거: git log(오늘 33커밋, 전부 origin/main 푸시·Vercel 배포 완료) + 세션 내 실측.
> 배포: https://dokdo-hq.vercel.app · 저장소: yelmovie/dokdo-exploration-hq

## 완료한 작업 (결과 기준)

- **QA 스윕 3차 완료**: 전 씬 힌트 잘림(위로 펼침), 슬롯 카드 수납, 캐릭터·말풍선 재배치, 패널 내용맞춤(auto+max), 확인 버튼 밑단 잘림 0건 실측.
- **5p 바위섬 분석실 전면 개편**: "위층/아래층" 표현 제거 → 단순화 단면 모형 프레이밍 + 3D 번호 배지 + 연결 활동 + 메타 문항 (총 6단계). 연결 조작은 **탭-탭 기본 + 드래그 보조**(초등 실기기 피드백 반영), E2E 4종 조작 통과.
- **다국어 완성(한/영/중/일)**: DOM 치환 엔진(I18nManager) + 사전 3종(en 794·zh 876·ja 840키, 병렬 에이전트 생성). 일본식 지명 표기 파일 전수 0건. 언어 전환 무한 루프 버그 수정(takeRecords+동일값 무기록).
- **도감 15장(5×3) 완성**: 아이콘 9종 신규(82~90), 태정관 카드→독도경비대(오늘) 교체, 독도 등대(4p 보상)·독도의용수비대(9p 보상) 추가. 전 미션 보상 카드 보장.
- **별점·점수 로직 전면 제거** (StatsManager → _unused/).
- **수료증**: 고화질 프레임 교체(1672×941), 학년반번호 표기 확인, blob 다운로드 + 인앱브라우저 미리보기 폴백, **학교·이름 손글씨 칸**(개인정보 미수집 원칙).
- **9p 발표 녹음**: 앱 내 동의 모달 → 마이크 녹음 + 들어보기, 닫힘 시 마이크 해제 보장.
- **프로덕션 장애 2건 해결**: 수료관 렌더 실패(fieldOf 잔존 — 미푸시 수정이 원인), 언어 선택 후 멈춤(번역 루프).
- **score-check 성적표 88/100** (docs/reviews/SCORE-20260808.md) + 개선 플랜 P1~P7.
- **스킬 6종 업그레이드**: touch/tablet/ship/score/learning/fun-check 에 "실전 교훈 — 2026-08 독도" 섹션 추가 (C:\Users\bongb\.claude\skills).
- **옵시디언 설계 노트** 작성: docs/classmakerlab-독도탐사본부-설계노트.md

## 테스트·검증 결과 (실측)

| 항목 | 결과 |
|---|---|
| 11개 씬 로드 + 콘솔 | 에러 0 |
| 버튼 잘림·겹침 전수 스윕 | 0건 |
| 5p 연결 E2E (탭-탭 정/역·점 탭·드래그) | 통과 |
| 다국어 3종 전 씬 한국어 잔존 | 0 (캔버스 수료증만 한국어 유지) |
| 언어 전환 루프 재현 테스트 | 2초간 mutation 0~1건 (수정 전 무한) |
| 프로덕션 배포 확인 | 최신 파일 fetch 200, tapSelect·takeRecords 코드 확인 |
| 초기 로딩 실측 | 3.66s / 6.12MB (폰트 4MB가 주범) |
| 마이크 실기기·FPS | 미실행 — 실기기 필요 |

## 확정된 결정

- 별점·점수 없음(완주·수집 중심) / 이름·학교는 손글씨(개인정보 미수집) / 다국어는 DOM 치환 방식 / 태정관 지령은 학습 내용 유지·도감 카드만 교체 / 원본 에셋은 pictures/ 보관.

## 남은 문제

| 등급 | 문제 | 재현 | 비고 |
|---|---|---|---|
| 중 | 첫 전송 6.1MB·로딩 3.7s | 네트워크 탭 | P1 폰트 한글 서브셋으로 ~2.8MB 예상 |
| 하 | 수료증 캔버스 문구 한국어 고정 | 다국어 모드에서 수료증 저장 | 언어별 캔버스 문구 별도 처리 필요 시 |
| 하 | 4p 해도 비율 변경(4:3)에 따른 부표 좌표 미세 확인 | 4p 육안 | 사용자 확인 대기 |
| 하 | 단면 GLB(pictures/dokdo_section_src.glb, 84MB) 미통합 | — | 최적화 후 5p 단면 모드 교체 예정 |

## 다음에 할 일 (우선순위)

1. P1: 폰트 서브셋 + 63.png/cert_frame/BGM 최적화 (`asset-audit`)
2. 실기기 검증 1회: 마이크·FPS·터치 (`touch-check`)
3. 단면 GLB 최적화 → 5p 통합
4. `ship-check` 판정 + score-check 재채점 (95+ 목표)

## 건드리면 안 되는 것

- `?v=` 에셋 버전(현재 v=4): 에셋 교체 시 반드시 숫자 증가 (Vercel immutable 캐시가 404까지 캐싱)
- serve.py ThreadingHTTPServer (BGM 스트림이 단일 스레드 점유)
- I18nManager 의 "동일값 무기록 + takeRecords" — 제거하면 언어 전환 무한 루프 재발
- 다국어 사전의 용어 원칙(Dokdo/East Sea 계열) — 수정 시 금지어 전수 스캔 필수
